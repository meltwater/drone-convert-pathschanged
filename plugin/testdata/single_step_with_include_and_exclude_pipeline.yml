kind: pipeline
type: docker
name: default

steps:
- name: message
  image: busybox
  commands:
  - echo "CHANGELOG.md was changed, README.md was not changed"
  when:
    paths:
      include:
      - CHANGELOG.md
      exclude:
      - README.md
