kind: pipeline
type: docker
name: default

steps:
- name: message
  image: busybox
  commands:
  - echo "This step will be excluded when README.md is changed"
  when:
    paths:
      exclude:
      - README.md
