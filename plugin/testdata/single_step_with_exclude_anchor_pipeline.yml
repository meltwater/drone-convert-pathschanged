kind: pipeline
type: docker
name: default

commands: &commands
  commands:
  - echo "This step will be excluded when README.md is changed"

steps:
- <<: *commands
  name: message
  image: busybox
  when:
    paths:
      exclude:
      - README.md
